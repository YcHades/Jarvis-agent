```json
{
    "需求澄清与工具边界探测": "在任务开始时，对于可能存在歧义的术语或标准，应主动向用户提问进行澄清。例如，询问是所有条件都必须满足，还是满足其中一部分即可，或者是否存在优先级；对于量化指标，例如“至少X个”，应澄清是“正好X个”还是“X个或更多”。更重要的是，对于任务中涉及的任何外部数据或需要使用特定工具的环节，应主动探测其可用性和自身工具集的能力边界，特别是在处理特定数据格式（如PDF、复杂图像）时，必须优先验证当前环境是否具备相应的工具和能力。如果缺乏必要工具（如 `pdftoppm`）且无法自行安装，应立即向用户声明此技术障碍，并请求提供更易处理的数据格式或安装相关工具的权限/指导。**绝不能在无法获取真实数据的情况下进行“模拟”或“猜测”操作。当明确识别到技术限制导致任务无法继续时（如无法处理PDF），应立即声明“硬停止”，并向用户解释具体技术障碍及所需帮助，而非在已知不可行的情况下反复尝试。**如果可能，应在筛选前与用户确认筛选标准，或在任务进行中分享部分初步筛选结果，以获得早期反馈，避免最终结果与用户预期严重偏离。当从外部信息源（如聊天记录）获取路径、文件名或其他关键数据时，该信息可能与实际系统状态存在差异。在进行关键操作前，应主动登录系统并通过视觉验证（如 `browser_extract_content` 查看文件列表）或命令行验证（如 `ls` 检查文件是否存在）来确认信息的准确性。在数据处理的每个阶段，都应进行中间结果的验证（如 `cat` 文件内容），以确保正确性，避免下游任务基于错误数据进行操作。在生成包含特殊字符（如逗号`,`, 美元符号`$`) 的数据文件（如CSV）时，应特别注意Shell的转义规则和引用方式，以确保字面量输出。",
    "UI交互策略优先级与工具边界认知": "在进行浏览器UI自动化时，应遵循以下优先级策略，并时刻警惕工具的局限性：\n1.  **URL直接导航 (`browser_navigate`)**: 如果能通过已知URL直接到达目标状态（如Rocket.Chat私聊页面、OwnCloud特定目录），优先使用此方法，这通常比任何UI点击都更稳定和高效。对于Rocket.Chat私聊，即使通过搜索获得用户名，也应优先构造精确URL（例如 `http://the-agent-company.com:3000/direct/<username_cleaned_for_url>`）进行导航。\n2.  **命令行/API操作 (`run_cmd`)**: 如果目标平台提供WebDAV、REST API或命令行工具（如`curl`, `wget`），优先探索使用 `run_cmd` 进行自动化操作，这通常比模拟浏览器UI更可靠，尤其适用于文件传输和基础数据交互（如OwnCloud的文件下载和上传）。\n3.  **确定性UI元素交互 (`browser_click`, `browser_type_text`)**: 对于静态、固定位置的UI元素（如登录按钮、明确的文本输入框），结合 `browser_get_browser_state` 进行操作。**然而，对于动态或不稳定的UI元素（如聊天界面中的搜索结果、文件列表中的操作按钮），`browser_get_browser_state` 提供的索引可能不准确，需谨慎对待。在输入文本后，通常需要 `browser_send_keys('Enter')` 来提交。**\n4.  **视觉辅助定位与上下文判断 (`browser_extract_content`)**: 当需要定位动态或无明确索引的元素时，使用 `browser_extract_content` 进行高精度视觉查询。然而，要警惕视觉模型给出的索引可能与 `browser_click` 或 `browser_type_text` 实际可操作的索引不一致，或随着页面状态变化而失效。此时，应将其视为辅助信息，而非绝对指令。它特别适用于提取页面内容、聊天记录或通过视觉预览的PDF内容。\n5.  **尝试替代UI交互模式**: 如果直接点击特定按钮失败，考虑该功能在应用中的其他实现方式（如：选中文件后操作栏出现、右键菜单等）。\n6.  **最终手段：识别并声明工具边界**: **如果经过多种策略尝试后，仍无法可靠地自动化特定UI操作（例如OwnCloud的文件分享链接创建，因动态或复杂UI导致元素索引不稳定），应及时识别并声明工具集无法完成该任务，避免陷入无意义的循环尝试。这意味着，对于某些复杂、动态或对自动化工具不友好的Web界面，可能需要人工干预或更换更专业的自动化框架。**",
    "任务依赖管理与阻塞处理": "在执行多步骤任务时，应严格管理任务间的依赖关系。**在尝试执行某一步骤之前，必须确保所有关键前置条件（特别是获取决策所需的信息，如采购需求或数据处理所需格式）已经满足。**如果某一关键前置步骤（如数据获取或处理）因技术限制而受阻（例如，无法处理特定格式的文件，或缺乏筛选所需的信息），应立即声明阻塞，并清晰地向用户解释阻塞原因和所需支持（如请求替代数据格式或安装工具的权限）。**避免在已知限制下进行无效操作，确保每次行动都是有意义的。**在缺乏必要信息或标准的情况下尝试处理数据，会导致无效的尝试和资源浪费。"
}
```